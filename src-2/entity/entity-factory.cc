#include "entity-factory.hpp"

/*
class EntityFactory {
  public:
    static Entity CreateChest(int hp, int capacity);

  private:
  protected:
};

class EntityFactoryActive {
  public:
    static EntityActive CreateWarrior();

  private:
  protected:
};
*/

Entity EntityFactory::CreateThing() {
}

Entity EntityFactory::CreateChest(int hp, int capacity) {
}